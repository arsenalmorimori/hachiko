<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:cv="clr-namespace:Camera.MAUI;assembly=Camera.MAUI"
             xmlns:skia="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls"
             x:Class="Hachiko.MainPage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="#fefefe">

    <!-- Landscape: camera left (65%), controls right (35%) -->
    <Grid ColumnDefinitions="*,Auto"
          RowDefinitions="*"
          Padding="12">

        <!-- LEFT PANEL -->
        <Grid Grid.Column="0"
              RowDefinitions="*"
              Margin="0,0,10,0">

            <!-- CAMERA PANEL-->
            <Border Grid.Row="0"
                    StrokeThickness="1"
                    Stroke="#2a2a3a"
                    BackgroundColor="#111118">

                <!-- CAMERA BORDER  RADIUS -->
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="14"/>
                </Border.StrokeShape>

                <!-- CAMERA VIEW-->
                <Grid>
                    <cv:CameraView x:Name="CamView"
                                   CamerasLoaded="CamView_CamerasLoaded"
                                   HorizontalOptions="Fill"
                                   VerticalOptions="Fill"/>
                    <skia:SKCanvasView x:Name="OverlayCanvas"
                                       PaintSurface="OverlayCanvas_PaintSurface"
                                       HorizontalOptions="Fill"
                                       VerticalOptions="Fill"
                                       BackgroundColor="Transparent"/>

                </Grid>
            </Border>

        </Grid>

        
        
        

        <!-- RIGHT CONTROL -->
        <VerticalStackLayout Grid.Column="1"
                             Spacing="10"
                             WidthRequest="90"
                             VerticalOptions="Center"
                             
                             >

            <!-- STATS  -->
            <Grid RowDefinitions="*,*" RowSpacing="1">

                <Border Grid.Row="0" Padding="8,6" BackgroundColor="#f0f0f0" StrokeThickness="0" Margin="0,0,20,0">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="6"/>
                    </Border.StrokeShape>
                    <HorizontalStackLayout HorizontalOptions="Center" Spacing="10">
                        <Label Text="FPS" TextColor="#545454" FontSize="14" HorizontalOptions="Center" VerticalOptions="Center"/>
                        <Label x:Name="FpsLabel" Text="--" TextColor="#2a2a2a" FontSize="16" FontAttributes="Bold" HorizontalOptions="Center"/>
                    </HorizontalStackLayout>
                </Border>
                
                <Border Grid.Row="1" Padding="8,6" BackgroundColor="#f0f0f0" StrokeThickness="0">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="6"/>
                    </Border.StrokeShape>
                    <HorizontalStackLayout HorizontalOptions="Center" Spacing="10">
                        <Label Text="OBJ" TextColor="#545454" FontSize="14" HorizontalOptions="Center" VerticalOptions="Center"/>
                        <Label x:Name="ObjLabel" Text="0" TextColor="#2a2a2a" FontSize="16" FontAttributes="Bold" HorizontalOptions="Center"/>

                    </HorizontalStackLayout>
                </Border>
                
            </Grid>

        </VerticalStackLayout>
    </Grid>
</ContentPage>
